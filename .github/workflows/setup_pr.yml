# ═══════════════════════════════════════════════════════════════════
# Issue → PR 基础设施 (纯 YAML，不走 Python)
# 创建分支和 PR，assign PR 给 Agent 以触发 Webhook 服务
#
# 纯 GitHub API 调用，无需 checkout，运行在 GitHub-hosted runner 上
# 仅监听 issues:assigned 事件，避免 PR/Comment 事件产生无效运行
# ═══════════════════════════════════════════════════════════════════
name: Setup PR

on:
  issues:
    types: [assigned]

jobs:
  setup-pr:
    if: >-
      github.event.action == 'assigned' &&
      github.event.assignee.login == 'xylcm'

    permissions:
      contents: write
      issues: write
      pull-requests: write

    # 纯 API 调用，无需 self-hosted runner
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Create Branch and Draft PR
        id: setup
        env:
          GH_REPO: ${{ github.repository }}
        run: |
          ISSUE_NUMBER="${{ github.event.issue.number }}"
          BRANCH_NAME="feat/issue-${ISSUE_NUMBER}"

          # 1. 获取 Issue 标题用于 PR
          ISSUE_TITLE=$(gh issue view "${ISSUE_NUMBER}" --json title --jq '.title')

          # 2. 通过 Git Refs API 创建分支（幂等，无需本地 checkout）
          MASTER_SHA=$(gh api "repos/${GH_REPO}/git/ref/heads/master" --jq '.object.sha')
          gh api "repos/${GH_REPO}/git/refs" \
            -f ref="refs/heads/${BRANCH_NAME}" \
            -f sha="${MASTER_SHA}" 2>/dev/null || echo "Branch already exists"

          # 3. 创建或复用 draft PR（幂等，gh pr create 无需本地仓库）
          PR_NUMBER=$(gh pr list --head "${BRANCH_NAME}" --state open --json number --jq '.[0].number')
          if [ -n "${PR_NUMBER}" ]; then
            echo "PR #${PR_NUMBER} already exists, reusing"
          else
            echo "Creating draft PR"
            PR_URL=$(gh pr create \
              --title "#${ISSUE_NUMBER} ${ISSUE_TITLE}" \
              --body "Closes #${ISSUE_NUMBER}" \
              --base master \
              --head "${BRANCH_NAME}" \
              --draft)
            PR_NUMBER=$(echo "${PR_URL}" | grep -oE '/pull/[0-9]+' | grep -oE '[0-9]+')
          fi

          echo "Setup complete: PR #${PR_NUMBER} on branch ${BRANCH_NAME}"

          # 4. 给 PR 打标签
          PR_LABELS="stage:ready"
          ISSUE_LABELS=$(gh issue view "${ISSUE_NUMBER}" --json labels --jq '.labels[].name')
          if echo "${ISSUE_LABELS}" | grep -q "^fastlane$"; then
            PR_LABELS="${PR_LABELS},fastlane"
          fi
          TYPE_LABEL=$(echo "${ISSUE_LABELS}" | grep "^type:" | head -n 1)
          if [ -n "${TYPE_LABEL}" ]; then
            PR_LABELS="${PR_LABELS},${TYPE_LABEL}"
          fi
          gh pr edit "${PR_NUMBER}" --add-label "${PR_LABELS}"

          # 5. 更新 Issue 状态: backlog → in-progress（幂等）
          gh issue edit "${ISSUE_NUMBER}" \
            --add-label "status:in-progress" \
            --remove-label "status:backlog"

          # 6. 将 PR assign 给 Agent（BOT_PAT 确保触发 Webhook 事件）
          gh pr edit "${PR_NUMBER}" --add-assignee "xylcm"

          # 输出变量供后续 step 使用
          echo "pr_number=${PR_NUMBER}" >> "$GITHUB_OUTPUT"
          echo "issue_title=${ISSUE_TITLE}" >> "$GITHUB_OUTPUT"

      - name: Report Setup Failure
        if: failure()
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          GH_REPO: ${{ github.repository }}
        run: |
          ISSUE_NUMBER="${{ github.event.issue.number }}"
          gh issue edit "${ISSUE_NUMBER}" --add-label "human:help-needed"

          COMMENT_FILE=$(mktemp)
          printf '**PR Setup Failed**\n\nFailed to create branch/PR for this issue.\n\n**Run Details:** %s\n\nPlease review the logs.' "${RUN_URL}" > "${COMMENT_FILE}"
          gh issue comment "${ISSUE_NUMBER}" --body-file "${COMMENT_FILE}"
          rm -f "${COMMENT_FILE}"
